# PlainPhone — 下一階段方向 (SPEC-NEXT)

> **狀態：提案。** `SPEC.md` 仍描述目前已實作的東西。
> 本文件規定**方向與驗證條件**，**不規定實作方法**。Worker 只要讓驗證條件成立，怎麼寫自由發揮。
> 與 `SPEC.md` 衝突時以本文件為準（廢止清單在 §8）。
> 依據：Owner 決策對話 2026-09-02 ＋ 同日實體裝置簽章實測。

---

## 1. 產品是什麼（修訂）

> **把 Home Screen 上的「圖示＋名字」變成只剩名字。**

一份純文字的 app 超連結清單，長駐桌面。解鎖就看得到，點一下就開。

**名字就是 app 自己的名字。** 這不是一個「用自己的話重寫清單」的產品；重新命名是可有可無的副作用，不是目的。

**修正記錄。** 舊 `SPEC.md` §1 寫成「安靜的辨識 / recognition without recall」，U-3 要求「每一列用擁有者自己的話陳述」。Owner 於 2026-09-02 否定：那是過度詮釋。真正的需求是——**視覺上不要圖，要字**。

**單一使用者：**Owner，iPhone 14 Pro，自行安裝。無第二使用者、無上架、無 onboarding。

---

## 2. Owner 決定（2026-09-02，已拍板）

| ID | 決定 |
|---|---|
| **W-1** | **形態是桌面 Widget。** 不做「先開 app 才看得到清單」的版本。解鎖即見是重點。 |
| **W-2** | **版面參數由使用者在 app 內調整。** 至少：字級、行高、字距、欄數。 |
| **W-3** | **欄數可選 1 / 2 / 3，預設 2。** 多欄是正常版面，不是例外。 |
| **W-4** | **切換清單一律用 iOS 原生機制**：桌面換頁，或 Widget Stack 上下滑。PlainPhone 不為此寫任何程式碼。 |
| **W-5** | **新增／刪除／排序會頻繁使用**，必須即時反映到 widget。 |
| **W-6** | **跳轉速度不設目標。** 接受 iOS 捷徑本身的速度。不再測「快不快」，只測「開不開得了」。 |
| **W-7** | **Dock 不在範圍內。** 不描述、不要求、不驗證。 |
| **W-8** | **啟動路徑：A 優先，B 備援。**（§4） |
| **W-9** | **共享儲存：先試 keychain，不行再付 $99。**（§5） |

---

## 3. 平台事實（不可協商）

| ID | 事實 |
|---|---|
| **F-1** | 免費個人團隊**無法取得 App Group**。Xcode 自動簽章產生的 profile 中 `application-groups` 為空陣列；改用 bundle 前綴命名仍然失敗。`[2026-09-02 實測]` |
| **F-2** | 同一份 profile **有**發 `keychain-access-groups: <TEAMID>.*`。這是免費帳號下已確認存在的跨 process 通道。`[2026-09-02 實測]` |
| **F-3** | 免費簽章**每 7 天過期**。過期後 iOS 不啟動 app，**也不啟動 widget extension**。 |
| **F-4** | 免費團隊每台裝置限 **3 個 app**、每 7 天限 **10 個 App ID**。widget extension 自己算一個，PlainPhone 佔 2。 |
| **F-5** | `Link` / `.widgetURL` 的文件定義是**把 URL 交給容器 app**，不是交給系統。 |
| **F-6** | `OpenURLIntent` 的文件是針對 universal link 寫的。自訂 scheme 能否成立**無保證**。 |
| **F-7** | `Link` 只在 `systemMedium` / `systemLarge` 提供每列獨立點擊區；`systemSmall` 只有一個。 |
| **F-8** | iOS **無法列舉已安裝的 app**。捷徑 app 可以，因為它是系統 app。 |
| **F-9** | 原生 iOS 18 本來就能移除所有桌面圖示、隱藏頁面、關閉 badge。**「安靜」是免費的**；這個產品必須拿「多出來的那份文字清單」自我證成。 |

---

## 4. 啟動機制

### M-1　統一機制，零特例

**一列 = 一個 app 名 = 一個同名捷徑。** 每一列都走 `shortcuts://run-shortcut?name=<列名>`，不做 per-app 特例。

理由（Owner）：**不是每個 app 都有 scheme，但每個捷徑都有。** 捷徑的「打開 App」可以開任何已安裝的 app（F-8），是唯一能均勻覆蓋全部清單的機制。

推論：
- **不建立 app scheme 資料庫**（沿用舊 SPEC 非目標）。
- 新增一列只需打 app 名字，URL 自動推導，不要要求使用者手押 URL。
- 使用者必須在捷徑 app 裡建立同名捷徑。這是一次性設定成本，**必須寫進 `README.md`**，不得隱瞞。
- 自由填 URL 的能力保留（例如 `https://`、`mailto:`），但不是主路徑。

### M-2　路徑 A（首選，先試這條）

widget 的一列直接讓系統開捷徑 URL（現行實作的 `OpenURLIntent`，或任何等效方式）。

- 優點：少一跳，**不會閃過 PlainPhone**。
- 風險：F-6，自訂 scheme 無保證。

### M-3　路徑 B（備援，A 失敗才用）

1. widget 一列 `Link` 到 **PlainPhone 自己的 scheme**（例如 `plainphone://open?shortcut=Beeper`）。F-5 保證 URL 會交給容器 app。
2. PlainPhone 接到 URL，再從 app 內開捷徑 URL。app 內開自訂 scheme 是完全支援的行為。
3. 捷徑執行「打開 App」。

**不要把捷徑 URL 直接放進 `Link`。** scheme 不是自己的時，iOS 會怎麼路由沒有文件保證，此路徑的全部價值就在於「每一步都在文件保證內」。

代價：會閃一下 PlainPhone。舊 SPEC D-3 已接受這個代價，且 W-6 不設速度目標。

### M-4　選哪條由實測決定

先裝 A，在裝置上實測。成功就用 A，失敗就換 B。結果（包含失敗的錯誤碼）必須寫進 `docs/device/`。不得因為 B 比較安全就跳過 A。

---

## 5. 共享儲存

### S-1　要求（機制不指定）

app 與 widget 必須讀寫**同一份資料**：清單內容＋版面設定。app 內修改後，widget 必須反映變更（W-2、W-5）。

這是需求，不是實作方式。Worker 可以用任何手段達成。

### S-2　免費帳號下的路徑（Owner 決定）

App Group 在免費帳號下不可用（F-1）。順序是：

1. **先試 keychain 共享群組**（F-2 確認可用）。資料量極小（＜1KB）。
2. 實作或實測證明不可靠（讀不到、餐延遲、鎖定問題等）→ **回報 Owner，由 Owner 決定是否付 $99 改用 App Group。**

**不得自行決定付費。** 不得為了逃避這個問題而放棄 W-5（改成「重編才生效」）。

不管用哪種機制，**降級狀態必須在 app 內明示**，不得靜默（沿用舊 K-13）。

---

## 6. 版面設定與容量

### L-1　可調參數

字級、行高、字距、欄數（1/2/3，預設 2）。設定位於 app 內，屬於共享資料（S-1）。

參數範圍自訂，但必須讓 Owner 能做出原型 `docs/product/prototype-core-flow.html` 里試過的版面。

### L-2　容量是函數，不是常數

舊 SPEC 寫死 `rowsPerPage` 為 large 12 / medium 5，並由此推出項目上限 15。**參數一旦可調，這些常數全部失效。**

容量必須從目前設定推導，並且**推導出來的值要能被測**（見 G1-3）。

### L-3　不得靜默裁切

設定變更導致容量下降時，可能出現「存了但哪一頁都看不到」的項目。

**不得靜默發生。** Worker 可以選：（a）依設定限制可儲存數量，或（b）允許儲存但在 app 內明確標示哪些看不到。兩者皆可，驗證條件相同（G1-4）。

---

## 7. 三道關卡

依序執行。**前一關未過，不得開始下一關的工作。**

### Gate 0　點下去開不開得了（阻斷性）

唯一問題：桌面 widget 的一列，點下去能不能開到目標 app。

這一關**不需要**共享儲存、不需要編輯功能、不需要可調版面。清單走程式碼種子即可。

目前建置會因 App Group entitlement 而失敗（F-1）。**Gate 0 期間允許直接移除該 entitlement**，這是暫時措施，不是最終設計。

| ID | 驗證 | 怎麼算過 |
|---|---|---|
| **G0-1** | 在實體 iPhone 上安裝成功，widget 可加到桌面並顯示清單 | 截圖 |
| **G0-2** | 清單中**每一列**點下去都開到正確的 app | 螢幕錄影，不是描述 |
| **G0-3** | 記錄實際成立的是路徑 A 還是 B；A 失敗時連錯誤碼一併記錄 | `docs/device/` 下的文件 |
| **G0-4** | `README.md` 記載實際行為，包含會不會閃過 PlainPhone | 文字與錄影一致 |

**Gate 0 失敗（A、B 都不行）→ 停下來回報 Owner。** 不要自行發明第三種機制，那代表 widget 形態不成立，是產品決策。

### Gate 1　每天能用

W-2、W-3、W-5、S-1 所需的一切。

| ID | 驗證 | 方式 |
|---|---|---|
| **G1-1** | app 內新增／刪除／排序，回桌面後 widget 反映變更 | 裝置實測，記錄實際延遲（**不設目標值**，只要記錄） |
| **G1-2** | app 內調字級／行高／字距／欄數，widget 反映變更 | 裝置實測 |
| **G1-3** | 任一組設定下，widget **實際渲染出來**的列數等於宣告容量 | 從渲染影像計數（沿用現有 `WidgetVisualTests` 手法），至少涵蓋 欄數1/2/3 × medium/large × 預設與最大字級 |
| **G1-4** | 不存在「已儲存、任何頁面都渲染不出來、且 app 內沒有告知」的項目 | 自動化測試 |
| **G1-5** | 共享通道失效時，app 內明示降級狀態 | 自動化測試 ＋ 畫面 |
| **G1-6** | 資料毀損不靜默覆寫；舊版資料仍能解碼 | 沿用舊 K-11 / K-14 測試 |
| **G1-7** | 簽章到期日在 app 內可見 | 沿用舊 N-2 測試 |
| **G1-8** | widget 上沒有任何數字、計數、頁碼、狀態、時間 | 沿用舊 N-3 測試 |
| **G1-9** | `README.md` 記載：16 個同名捷徑的建立方式、7 天過期、3 app 限制、Widget Stack 設定方式 | 文件審閱 |

### Gate 2　兩週

| ID | 驗證 |
|---|---|
| **G2-1** | 連續兩週日常使用，每天一行手寫紀錄：「今天我偏好這份清單勝過空白桌面嗎？」 |

**若 Gate 2 不成立，正確的做法是退場，不是加功能。**

---

## 8. 相對 `SPEC.md` 的廢止與修改

| 舊條目 | 處置 |
|---|---|
| **U-3**（每一列用擁有者自己的話） | **廢止。** 名字就是 app 本名（§1）。 |
| **N-9**（單一 `Open` 捷徑＋ input） | **修改為**一 app 一捷徑，名稱相同，不帶 input（M-1）。 |
| **K-8**（透過 App Group 共享） | **修改為**「app 與 widget 必須共享同一份資料」，機制不指定（S-1）。 |
| **CH-3**（rows-per-page 寫死 12 / 5） | **廢止。** 容量是設定的函數（L-2）。 |
| **CH-1**（項目上限 15） | **廢止。** 上限隨設定變動，規則由 L-3 取代。 |
| **CH-4**（名稱 24 字） | 保留上限的想法，具體數字交給 Worker；多欄版面下需重新判斷。 |
| **D-7**（免費帳號能不能拿 App Group） | **關閉：不能。** 見 F-1。 |
| **D-3**（閃過 PlainPhone 可不可接受） | **保持關閉：可接受。** 但 A 路徑優先（M-4）。 |
| **T1**（測跳轉速度） | **降級。** 只測能不能開（G0-2），不測快不快（W-6）。 |
| **T0**（一個捷徑還是十六個） | **關閉：十六個。** Owner 決定，不需實測。 |
| **P-1 / P-2**（分頁與 Widget Stack） | **保留。** 切換一律用原生機制（W-4）。 |
| **RM-3**（不出 `systemSmall`） | **保留。** F-7。 |
| Dock 相關描述（N-1 一部分） | **移出範圍。** W-7。 |

其餘 `SPEC.md` 條目（K-1、K-2、K-9、K-10、K-11、K-12、K-14、N-2、N-3、N-4、N-6、N-8、§4 非目標、§6 隱私、§8 工程約束）**全數保留。**

---

## 9. 新增的非目標

除了 `SPEC.md` §4 已列的，再加：

- **不在 widget 內做自制的滑動、分頁或切換手勢。** 一律用 iOS 原生（W-4）。
- **不做 app 列舉、捷徑列舉、或任何自動匯入。** F-8 不允許。
- **不為了避開共享儲存而取消即時編輯。** W-5 是硬需求。
- **不自行決定花錢。** 付費與否是 Owner 的決定（S-2）。
- **不做 Dock。** W-7。

---

## 10. Worker 執行規則

1. **依序過關。** Gate 0 未過不得做 Gate 1 的任何工作。
2. **驗證是規格，實作不是。** 本文件不規定用什麼 API、什麼結構。能過驗證就行。
3. **不得將模擬器結果當作裝置行為回報。** 沒實際跑過的檢查不得聲稱通過。
4. **不可逆或產品定義的決定要問 Owner。** 付費、放棄需求、換形態都屬於此類。
5. **刪除是合法的改動。** 不需要補上代償性的新功能。
6. **只在本專案目錄內工作。** 不讀憑證，不改全域開發者設定，不用 `sudo`，不改 `xcode-select`。
7. **建置與測試一律加前綴** `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer`。
8. **每過一關，把結果（含不利的結果）寫回 `SPEC.md` 與 `README.md`。** 文件與實際行為不一致視同未完成。

### 現狀備註（2026-09-02）

- 裝置已註冊，provisioning profile 已能產生。
- 目前 `xcodebuild` 失敗原因僅為 App Group entitlement（F-1）。移除後應可建置。
- 現行程式碼的清單種子在 `Shared/DefaultLayouts.swift`，仍使用舊的單一 `Open` 捷徑形式，需依 M-1 改成一 app 一捷徑。
- 版面參數的目標手感可參考 `docs/product/prototype-core-flow.html`。
